pandas
numpy

V9.1.2 FIX: yfinance 0.2.54 -> >=0.2.58 (multi-index DataFrame bugs fixed)

yfinance>=0.2.58
openpyxl
matplotlib
reportlab
requests
deep-translator
python-dotenv

--- V8.2.0: Breaking news (RSS feeds) ---

feedparser

--- V8.2.0: Render 24x7 health server (/ping for UptimeRobot) ---

flask
gunicorn

--- V8.2.0 Update: Additional free NSE data source (no auth needed) ---

V9.1.2 FIX: nse[server] extra optional hai aur Render par install fail

kar sakta hai. Safe version: plain nse package (no extras).

jugaad-data
nse

--- V8.2.0: ZoneInfo timezone data (Windows par stdlib tzdata nahi hota;

Linux/Mac par bhi harmless. IST timezone ke liye zaroori) ---

tzdata

--- V9.1.1: GLM API retry (exponential backoff for 429 handling) ---

tenacity

--- V10.2: Google Gemini AI (Replaced GLM due to balance issues) ---

google-generativeai
